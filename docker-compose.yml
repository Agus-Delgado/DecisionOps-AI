version: "3.9"

services:
  api:
    build:
      context: ./apps/api
    ports:
      - "8000:8000"
    environment:
      - PORT=8000

  web:
    build:
      context: ./apps/web
      args:
        VITE_API_BASE: http://localhost:8000
    ports:
      - "5173:80"
    depends_on:
      - api
